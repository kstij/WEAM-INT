import React from 'react';
import WeamLogo from './WeamLogo';

const WeamNavigation = ({ appName = '<%= appName %>' }) => {
  const handleBackToApp = () => {
    window.location.href = 'https://app.weam.ai';
  };

  return (
    <nav className="bg-white shadow-sm border-b border-gray-200">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-16">
          {/* Left side - Logo and App Name */}
          <div className="flex items-center">
            <WeamLogo size="medium" />
            <div className="ml-4">
              <h1 className="text-lg font-semibold text-gray-900">
                <%= appName %>
              </h1>
              <p className="text-sm text-gray-500">
                <%= category %> • Powered by Weam AI
              </p>
            </div>
          </div>

          {/* Right side - Back to App button */}
          <div className="flex items-center space-x-4">
            <button
              onClick={handleBackToApp}
              className="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200"
            >
              <svg className="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 19l-7-7m0 0l7-7m-7 7h18" />
              </svg>
              Back to App
            </button>
          </div>
        </div>
      </div>
    </nav>
  );
};

export default WeamNavigation;
